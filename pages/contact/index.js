import ContactContainer from './ContactContainer';
import MarkeringLayout from '../../layouts/MarketingLayout';
import withLayout from '../../hocs/withLayout';

export default withLayout(ContactContainer, MarkeringLayout);
